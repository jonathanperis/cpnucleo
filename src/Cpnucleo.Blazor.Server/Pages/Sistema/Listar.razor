@page "/sistema/listar"

@inject NavigationManager NavigationManager
@inject IAppService<SistemaViewModel> SistemaAppService

@if (lista == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>Relação de Sistemas</h5>
                    <div class="ibox-tools">
                        <a class="fullscreen-link">
                            <i class="fa fa-expand"></i>
                        </a>
                    </div>
                </div>
                <div class="ibox-content">
                    <NavLink class="btn btn-primary btn-sm" role="button" href="/sistema/incluir">+ Sistema</NavLink>
                    <br /><br />
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered table-hover table-datatable">
                            <thead>
                                <tr>
                                    <th>Id</th>
                                    <th>Nome</th>
                                    <th>Descrição</th>
                                    <th>Data de Inclusão</th>
                                    @if (lista.Count() > 0)
                                    {
                                        <th class="no-sort"></th>
                                        <th class="no-sort"></th>
                                    }
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in lista)
                                {
                                    <tr>
                                        <td>@item.Id</td>
                                        <td>@item.Nome</td>
                                        <td>@item.Descricao</td>
                                        <td>@item.DataInclusao.Value.ToString("dd/MM/yyyy")</td>
                                        @if (lista.Count() > 0)
                                        {
                                            <td style="text-align: center">
                                                <button class="btn btn-outline btn-warning btn-xs" @onclick="@(() => NavigationManager.NavigateTo($"/sistema/alterar/{item.Id.ToString()}"))">Alterar</button>
                                                @*<a class="btn btn-outline btn-warning btn-xs" role="button" @oncl onclick="@AlterarSistema(item.Id)">Alterar</a>*@
                                                @*<NavLink class="btn btn-outline btn-warning btn-xs" role="button" @onclick="@(() => AlterarSistema(item.Id))">Alterar</NavLink>*@
                                            </td>
                                            <td style="text-align: center">
                                                <button class="btn btn-outline btn-danger btn-xs" @onclick="@(() => NavigationManager.NavigateTo($"/sistema/remover/{item.Id.ToString()}"))">Remover</button>
                                                @*<a class="btn btn-outline btn-danger btn-xs" role="button" href="/sistema/remover/@item.Id">Remover</a>*@
                                                @*<NavLink class="btn btn-outline btn-danger btn-xs" role="button" @onclick="RemoverSistema">Remover</NavLink>*@
                                            </td>
                                        }
                                    </tr>
                                }
                            </tbody>
                            <tfoot>
                                <tr>
                                    <th>Id</th>
                                    <th>Nome</th>
                                    <th>Descrição</th>
                                    <th>Data de Inclusão</th>
                                    @if (lista.Count() > 0)
                                    {
                                        <th></th>
                                        <th></th>
                                    }
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
}

@code {
    IEnumerable<SistemaViewModel> lista;

    protected override async Task OnInitializedAsync()
    {
        lista = await Task.Run(() => SistemaAppService.Listar());
    }
}
