@page "{id:Guid}"

@model Impedimento.AlterarModel
@{
    ViewData["Title"] = "Alteração de Impedimento";
}

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-filled">
            <div class="panel-heading">@ViewData["Title"]</div>
            <div class="panel-body">
                <form method="post">
                    <div class="form-group">
                        <label asp-for="Impedimento.Id"></label>
                        <input type="hidden" asp-for="Impedimento.Id">
                        <p class="form-control-static">@Html.DisplayFor(x => x.Impedimento.Id)</p>
                    </div>

                    <div class="form-group">
                        <label asp-for="Impedimento.Nome"></label>
                        <input type="text" class="form-control" asp-for="Impedimento.Nome" placeholder="@Html.DisplayNameFor(x => x.Impedimento.Nome)">
                        <span class="help-block m-b-none">@Html.ValidationMessageFor(x => x.Impedimento.Nome)</span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Impedimento.DataInclusao"></label>
                        <input type="hidden" asp-for="Impedimento.DataInclusao">
                        <p class="form-control-static">@Html.DisplayFor(x => x.Impedimento.DataInclusao)</p>
                    </div>

                    <div class="form-group">
                        <a class="btn btn-default" role="button" asp-page="Listar">Voltar</a>
                        <button class="btn btn-accent" type="submit">Alterar</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}