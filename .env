# Database credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=cpnucleo

# Connection string for API services (using variable substitution)
API_CONNECTION_STRING=Host=db;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD};Database=${POSTGRES_DB};Minimum Pool Size=10;Maximum Pool Size=10;Multiplexing=true